# limit

Rust-based code agent with Anthropic support.

## Crates

- **limit-llm**: Unified LLM API
- **limit-agent**: Agent runtime with tool calling
- **limit-cli**: Interactive REPL interface
- **limit-tui**: Terminal UI library

## Development

```bash
cargo build --workspace
cargo test --all
cargo run --package limit-cli
```
